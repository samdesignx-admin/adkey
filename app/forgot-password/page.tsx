"use client";
import { FormEvent,useState } from "react";
import Link from "next/link";
import { createClient } from "@/lib/supabase/client";

export default function ForgotPassword(){const [email,setEmail]=useState(""),[loading,setLoading]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage("");const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setLoading(false);setMessage(error?"We couldn't send the reset link. Please try again.":"If an Adkey account exists for that email, you'll receive a password reset link shortly.");}return <main className="container" style={{maxWidth:520,paddingTop:80}}><div className="logo">Ad<span>key</span></div><div className="kicker" style={{marginTop:42}}>Account recovery</div><h1 style={{fontSize:50,marginTop:12}}>Reset your password.</h1><p className="lead">Enter your email and we'll send you a secure Adkey password reset link.</p><form className="card" onSubmit={submit}><input required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com" type="email" style={{width:"100%",padding:14,border:"1px solid #ddd",borderRadius:10,marginBottom:16}}/><button disabled={loading} className="btn btn-yellow" style={{width:"100%"}}>{loading?"Sending...":"Send reset link"}</button>{message&&<p style={{lineHeight:1.5}}>{message}</p>}<p style={{textAlign:"center"}}><Link href="/login" style={{fontWeight:800}}>Back to log in</Link></p></form></main>;}